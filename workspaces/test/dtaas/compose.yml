services:
  workspace:
    image: workspace:latest
    container_name: workspace
    build:
      context: ../..
      dockerfile: Dockerfile.ubuntu.noble.xfce
    environment:
      - MAIN_USER=${USERNAME1:-user1}
    ports:
      - "80:8080"
    shm_size: 512m
    volumes:
      - ./files/user1:/workspace
      - ./files/common:/workspace/common
    develop:
      watch:
        - path: .
          action: rebuild
